RSpec.describe Test::Remote::Redshift do
  it "has a version number" do
    expect(Test::Remote::Redshift::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
